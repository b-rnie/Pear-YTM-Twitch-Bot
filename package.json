{
  "name": "twitch-music-bot",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/bundle.cjs",
  "scripts": {
    "start": "node bot.js",
    "build": "esbuild bot.js --bundle --platform=node --target=node20 --outfile=dist/bundle.cjs --format=cjs"
  },
  "dependencies": {
    "ws": "^8.21.1",
    "yt-search": "^2.13.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
